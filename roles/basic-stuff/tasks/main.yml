---
- name: Install basic stuff
  ansible.builtin.package:
    name:
      - ansible
      - btop
      - htop
      - vim
      - curl
      - wget
      - git
      - gnupg
      - autoconf
      - build-essential
      - libncurses-dev
      - libssl-dev
    state: present
